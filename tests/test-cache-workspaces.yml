name: Test cache-workspaces input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Create a minimal Cargo workspace
        run: |
          mkdir -p /tmp/test-workspace/src
          cat > /tmp/test-workspace/Cargo.toml <<'EOF'
          [package]
          name = "test-workspace"
          version = "0.1.0"
          edition = "2021"
          EOF
          echo 'fn main() {}' > /tmp/test-workspace/src/main.rs
      - id: action
        uses: .
        with:
          toolchain: stable
          cache: "true"
          cache-workspaces: /tmp/test-workspace -> target
      - name: Assert rustc-version output is set
        env:
          RUSTC_VERSION: ${{ steps.action.outputs.rustc-version }}
        run: |
          echo "rustc-version: $RUSTC_VERSION"
          if [ -z "$RUSTC_VERSION" ]; then
            echo "FAIL: rustc-version output is empty"
            exit 1
          fi
          echo "PASS"
